Frontend (Vite + React)
========================

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Then open the dev server (shown by Vite) to view articles fetched from the Laravel API.
